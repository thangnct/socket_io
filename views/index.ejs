<!doctype html>
<html>

<head>
  <title>Socket.IO chat</title>

</head>

<body>

  <form action="">
    <input id="datahaha" type="text"> <button id="btn">Send</button>
  </form>
  <ul id="messages"></ul>
  <script src="/socket.io/socket.io.js"></script>
  <script src="https://code.jquery.com/jquery-1.11.1.js"></script>

  <script>
    var socket = io();
  </script>
  <script>
    $(function () {
      var socket = io();
      $('form').submit(function (e) {
        e.preventDefault(); // prevents page reloading
        socket.emit('chat message', $('#datahaha').val());
        // $('#m').val('');
        return false;
      });
      socket.on('chat message', function (msg) {
        $('#messages').append($('<li>').text(msg));
          // console.log("???")
        // reset();
      });

    });

    // function reset() {
    //   console.log("ss")
    //   $("#btn").click(function () {
    //     $("#datahaha").val("");
    //   });
    // }
  </script>
</body>

</html>